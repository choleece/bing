<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.choleece.bing.common.mapper.BaseRoleMapper">
    <select id="listRolePermissions" resultType="java.lang.String">
        select perms from id_card.sys_menu sm
        inner join id_card.sys_role_menu srm on sm.menu_id = srm.menu_id
        inner join id_card.sys_user_role sur on sur.role_id = srm.role_id
        where sur.user_id = #{userId}
    </select>
</mapper>